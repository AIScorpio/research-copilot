import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false, // Disable to suppress swagger-ui-react warnings
};

export default nextConfig;
